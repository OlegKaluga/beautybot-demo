# 🚀 Размещение BeautyBot Lite на PythonAnywhere

## Важно!
PythonAnywhere **не поддерживает** запуск Telegram-ботов на бесплатном тарифе напрямую (нет фонового режима).  
Есть **два варианта**:

---

## ✅ Вариант 1: Платный тариф (рекомендуется)

На платном тарифе ($5/мес) можно запустить бота в фоне через **Always-on tasks**.

### Шаг 1: Загрузите файлы на PythonAnywhere

1. Зайдите в панель: https://www.pythonanywhere.com/user/Kaluga/files/home/Kaluga
2. Загрузите все файлы из папки `beautybot_lite_v1`:
   - `bot.py`
   - папки: `config`, `database`, `handlers`, `keyboards`, `middlewares`, `utils`
   - `.env`
   - `requirements.txt`
   - `nail_bot.db` (если есть данные)

**Способы загрузки:**
- Через веб-интерфейс (кнопка Upload)
- Через Git (если проект в репозитории)
- Через консоль Bash на PythonAnywhere

### Шаг 2: Установите зависимости

Откройте **Bash console** на PythonAnywhere и выполните:

```bash
cd /home/Kaluga/beautybot_lite_v1
pip install -r requirements.txt
```

### Шаг 3: Проверьте .env

Убедитесь, что файл `.env` содержит правильные данные:
- `BOT_TOKEN` — ваш токен бота
- `ADMIN_IDS` — ваш Telegram ID
- `DB_PATH` — путь к базе данных

### Шаг 4: Запустите как Always-on task

1. Перейдите в раздел **Tasks** → **Always-on tasks**
2. Нажмите **Add an always-on task**
3. Укажите:
   - **Source code directory**: `/home/Kaluga/beautybot_lite_v1`
   - **Virtualenv**: выберите Python 3.10+
4. В поле **Command** введите:
   ```
   python bot.py
   ```
5. Нажмите **Save**

Бот запустится автоматически и будет работать постоянно!

### Шаг 5: Проверьте логи

Логи будут в файле `/home/Kaluga/beautybot_lite_v1/bot.log`

---

## ⚠️ Вариант 2: Бесплатный тариф (ограничения)

На бесплатном тарифе **нет фонового режима**. Бот будет работать, только пока открыта консоль.

### Запуск из консоли:

```bash
cd /home/Kaluga/beautybot_lite_v1
python bot.py
```

**Минусы:**
- Бот останавливается при закрытии консоли
- Нужно держать вкладку открытой

---

## 🔧 Решение: Альтернативные бесплатные хостини

Для бесплатного размещения рассмотрите:

| Платформа | Плюсы | Минусы |
|-----------|-------|--------|
| **Railway.app** | Бесплатно, есть фон | Нужна карта |
| **Render.com** | Бесплатно, фон | Засыпает через 15 мин |
| **Fly.io** | Бесплатный лимит | Нужна карта |
| **VPS (timeweb, reg.ru)** | Полный контроль | ~150-300 руб/мес |

---

## 📝 Команды для PythonAnywhere

```bash
# Перейти в папку бота
cd /home/Kaluga/beautybot_lite_v1

# Установить зависимости
pip install -r requirements.txt

# Запустить бота (тест)
python bot.py

# Посмотреть логи
tail -f bot.log

# Проверить структуру
ls -la
```

---

## 🛠 Если что-то не работает

1. **Ошибка импорта**: Убедитесь, что все папки загружены
2. **Ошибка токена**: Проверьте `.env`
3. **База данных**: Убедитесь, что `nail_bot.db` существует
4. **Логи**: Смотрите `bot.log` для деталей

---

## 📞 Контакты поддержки PythonAnywhere

- Help: https://help.pythonanywhere.com/
- Forum: https://www.pythonanywhere.com/forums/
