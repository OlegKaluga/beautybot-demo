# 🚀 Инструкция по развёртыванию BeautyBot Lite v1.0

## 📋 Шаг 1: Подготовка

### 1.1. Проверка файлов

Убедитесь, что все файлы на месте:

```
beautybot_lite_v1/
├── bot.py ✓
├── config/ ✓
├── database/ ✓
├── handlers/ ✓
├── keyboards/ ✓
├── middlewares/ ✓
├── utils/ ✓
├── .env.example ✓
├── requirements.txt ✓
└── docs/ ✓
```

### 1.2. Требования

- Python 3.8 или выше
- Доступ в Интернет
- Telegram аккаунт

---

## 📋 Шаг 2: Создание бота в Telegram

### 2.1. Получение токена

1. Откройте @BotFather в Telegram
2. Отправьте `/newbot`
3. Введите имя бота (например, `Luxe Beauty Bot`)
4. Введите username бота (например, `LuxeBeautyBot`)
5. Скопируйте токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2.2. Настройка бота

В @BotFather:

1. `/setuserpic` — загрузите логотип салона
2. `/setdescription` — описание бота
3. `/setabouttext` — текст в About

---

## 📋 Шаг 3: Настройка проекта

### 3.1. Создание .env

Скопируйте `.env.example` в `.env`:

```bash
copy .env.example .env
```

### 3.2. Заполнение .env

Откройте `.env` и заполните:

```env
# ===== BOT =====
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_IDS=123456789

# ===== SALON BRANDING =====
SALON_NAME="Студия красоты «Luxe»"
SALON_LOGO="https://example.com/logo.png"
PRIMARY_COLOR="#FF6B9D"
WELCOME_TEXT="Добро пожаловать в {salon_name}! ✨ Запишитесь на процедуру за 2 минуты"

# ===== CHANNEL =====
CHANNEL_ID=
CHANNEL_LINK=

# ===== DATABASE & TIME =====
DB_PATH=nail_bot.db
TIMEZONE=Europe/Moscow

# ===== REMINDERS =====
REMINDER_TEXT="Напоминаем, что вы записаны на {service} завтра в {time}. Ждём вас! 💅"
```

### 3.3. Получение ADMIN_IDS

1. Откройте @userinfobot в Telegram
2. Нажмите Start
3. Скопируйте ваш ID
4. Вставьте в `ADMIN_IDS`

### 3.4. Настройка канала (опционально)

Если нужна обязательная подписка:

1. Создайте канал
2. Добавьте бота в канал (администратором)
3. Узнайте ID канала через @GetMyIDBot
4. Вставьте в `CHANNEL_ID`
5. Вставьте ссылку в `CHANNEL_LINK`

---

## 📋 Шаг 4: Установка зависимостей

### 4.1. Установка Python

Если не установлен:
- Скачайте с python.org
- Установите (отметьте "Add Python to PATH")

### 4.2. Установка пакетов

Откройте терминал в папке проекта:

```bash
cd beautybot_lite_v1
pip install -r requirements.txt
```

---

## 📋 Шаг 5: Первый запуск

### 5.1. Запуск бота

```bash
python bot.py
```

### 5.2. Проверка

В логах должно быть:
```
INFO - Scheduler started
INFO - 🚀 BeautyBot Lite запущен!
INFO - Start polling
```

### 5.3. Тестирование

1. Откройте бота в Telegram
2. Нажмите `/start`
3. Должно появиться меню:
   ```
   📅 Записаться   💰 Прайсы
   🖼 Портфолио   ❓ Помощь
   🗓 Мои записи  ⭐ Отзыв
   ```

---

## 📋 Шаг 6: Настройка админ-панели

### 6.1. Вход в админку

Отправьте боту `/admin`

Должно появиться меню:
```
➕ Добавить день    ❌ Закрыть день
⏰ Слоты            📋 Записи
✉️ Написать клиенту
📊 Общий отчёт
✂️ Стрижки          💅 Ногти
⭐ Отзывы           ⛔ Чёрный список
🔙 В меню
```

### 6.2. Добавление рабочих дней

1. Нажмите **"➕ Добавить день"**
2. Нажмите **"➕ Добавить все"** (добавится 30 дней)
3. Или выбирайте даты вручную

### 6.3. Проверка слотов

1. Нажмите **"⏰ Слоты"**
2. Выберите зал → мастера → дату
3. Должны отображаться слоты 10:00-19:00

---

## 📋 Шаг 7: Тестирование записи

### 7.1. Полная запись

1. Нажмите **"📅 Записаться"**
2. Выберите зал (✂️ Стрижки)
3. Выберите мастера (М.1)
4. Выберите услугу (Стрижка)
5. Выберите дату (сегодня или завтра)
6. Выберите время (10:00)
7. Введите имя (Тест)
8. Введите телефон (+79991234567)
9. Подтвердите запись

### 7.2. Проверка в админке

1. `/admin` → **"📋 Записи"**
2. Выберите дату
3. Должна появиться тестовая запись

### 7.3. Отмена записи

1. Нажмите **"🗓 Мои записи"**
2. Нажмите **"❌ Отменить"**
3. Запись должна исчезнуть

---

## 📋 Шаг 8: Настройка канала (опционально)

### 8.1. Если канал нужен

1. В `.env` укажите `CHANNEL_ID` и `CHANNEL_LINK`
2. Перезапустите бота
3. Проверьте, что требуется подписка

### 8.2. Если канал не нужен

Оставьте `CHANNEL_ID` пустым — проверка подписки отключится.

---

## 📋 Шаг 9: Автозапуск (для Windows)

### 9.1. Создание .bat файла

Создайте `start_bot.bat`:

```batch
@echo off
cd /d "%~dp0"
python bot.py
pause
```

### 9.2. Добавление в автозагрузку

1. Нажмите `Win + R`
2. Введите `shell:startup`
3. Создайте ярлык для `start_bot.bat`

---

## 📋 Шаг 10: Мониторинг

### 10.1. Логи

Файл `bot.log` содержит:
- Время запуска
- Ошибки
- Действия пользователей

### 10.2. Проверка БД

```bash
python check_db.py
```

Покажет:
- Количество рабочих дней
- Залы
- Мастеров
- Услуги

---

## ❓ Частые проблемы

### Бот не запускается

**Проблема:** Ошибка "ModuleNotFoundError"

**Решение:**
```bash
pip install -r requirements.txt
```

### Бот не отвечает

**Проблема:** Бот запущен, но не реагирует

**Решение:**
1. Проверьте токен в `.env`
2. Проверьте интернет
3. Перезапустите бота

### Ошибка "Conflict: terminated by other getUpdates"

**Проблема:** Бот запущен несколько раз

**Решение:**
```bash
taskkill /F /IM python.exe
python bot.py
```

### Клиент не может записаться

**Проблема:** Нет рабочих дней

**Решение:**
1. `/admin` → "➕ Добавить день"
2. Нажмите "➕ Добавить все"

### Слоты не отображаются

**Проблема:** Слоты не созданы

**Решение:**
1. Удалите день: `/admin` → выберите дату
2. Добавьте заново

---

## ✅ Чек-лист готовности

- [ ] Токен получен и вставлен в `.env`
- [ ] ADMIN_IDS указан
- [ ] Зависимости установлены
- [ ] Бот запускается без ошибок
- [ ] Меню отображается
- [ ] Рабочие дни добавлены
- [ ] Тестовая запись создана
- [ ] Тестовая запись отменена
- [ ] Админ-панель работает
- [ ] Отчёты показывают данные

---

## 📞 Поддержка

При проблемах:

1. Проверьте `bot.log`
2. Проверьте `.env`
3. Перезапустите бота
4. Обратитесь к разработчику

---

**Версия инструкции:** 1.0  
**Дата:** Февраль 2026
